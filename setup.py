from setuptools import setup, find_packages

setup(
    name='ipypy',
    version='1.0.0',
    url='',
    description='ipypy notebook format',
    packages=find_packages(),
    install_requires=[],
)
